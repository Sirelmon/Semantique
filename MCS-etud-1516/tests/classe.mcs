public class Point {
	public int x;
	public int y;
	
	public Point(int a, int b){
		this.x = a;
		this.y = b;
		return this;
	}
	
	public void inverser() {
		int z = this.x;
		this.x = this.y;
		this.y = z;
	}
	
	public int somme() {
		return (this.x + this.y);
	}
}

typedef struct {
	int x;
	int y;
} Point2;

Point2 fonction(Point2 p, int a, int b) {
	p.x = a;
	p.y = b;
	return p;
}

void main() {
	Point p = new Point(p,1,2);
	p.inverser(p);
	int s = p.somme(p);
}